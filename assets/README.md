# Assets

Các hình trong thư mục này được trích từ output notebook đã chạy: phân bố nhãn, độ dài câu, confusion matrix và so sánh mô hình. Ảnh giao diện app (`app_preview.png`) sẽ được bổ sung sau khi chạy `streamlit run app.py` và chụp màn hình thật.
